import { Notifications } from '@/enums'
import { Notification } from '.'

export const getNotificationByCode = (code: typeof Notifications): Notification => {
  const _result = {} as Notification
  return _result
}
